name: remote ssh command
on: 
  label:
    types:
      - created
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v4
      - name: Check Container
        run: |
          ssh -t mlopstest.pem ubuntu@34.220.192.251
          pwd
