name: remote ssh command
on: 
  label:
    types:
      - created
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v4
      - name: show mlopstest.pem file content
        run: cat mlopstest.pem
          
